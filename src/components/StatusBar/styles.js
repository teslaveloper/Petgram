import styled from 'styled-components';

export const SBar = styled.div`
  display: flex;
  justify-content: space-between;
  padding: 5px;

  & .btn-logout {
    color: red;
  }

  & button {
    cursor: pointer;
  }
`
